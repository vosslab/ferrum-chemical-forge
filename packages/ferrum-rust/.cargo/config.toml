# Cargo discovers this configuration from both Ferrum Rust workspaces. Keep all
# ordinary developer compiler state in the repository-owned disposable build
# work area rather than beside source packages. Front doors that need isolated
# work state set CARGO_TARGET_DIR to another owned build/ child and clean it.
[build]
target-dir = "../../build/.cargo-target"
