"""Migration-only OASA compatibility surface.

The ordinary Ferrum-Qt application never imports this package.  Code that
still requires the historical session/canvas editor must name the compatibility
root explicitly so the remaining migration boundary is visible.
"""
