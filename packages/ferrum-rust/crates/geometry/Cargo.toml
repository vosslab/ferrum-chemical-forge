[package]
name = "ferrum-geometry"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# Geometry is deliberately expressed in Ferrum's own finite Point2 type.  These
# maintained libraries are adapters at the rendering and linear-algebra edges,
# not a second public geometry model.
kurbo = "*"
nalgebra = "*"
thiserror = "*"
