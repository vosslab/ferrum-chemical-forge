[package]
name = "ferrum-api-python-native"
version = "26.8.0"
edition = "2024"
rust-version = "1.97.1"
license = "LGPL-3.0-only"
publish = false

# This maturin project builds outside the Rust workspace so it can produce a
# self-contained Python extension wheel.
[workspace]

[lib]
name = "ferrum_chem"
crate-type = ["cdylib"]

[features]
# Wheel builds must not link libpython; `maturin` selects this default feature.
# Cargo test uses `--no-default-features` to embed and link the interpreter.
default = ["extension-module"]
extension-module = ["pyo3/extension-module"]

[dependencies]
ferrum-api = { path = ".." }
ferrum-chemistry = { path = "../../chemistry" }
ferrum-core = { path = "../../core" }
ferrum-document = { path = "../../document" }
ferrum-domain = { path = "../../domain" }
ferrum-geometry = { path = "../../geometry" }
ferrum-render = { path = "../../render" }
pyo3 = { version = "*", features = ["multiple-pymethods"] }
serde_json = { version = "*" }

[build-dependencies]
pyo3-build-config = { version = "*" }

# This standalone workspace shares the repository target directory. Keep useful
# line tables for Ferrum code while avoiding unbounded incremental object caches.
[profile.dev]
debug = 1
incremental = false

[profile.dev.package."*"]
debug = 0

[profile.test]
debug = 1
incremental = false

[profile.test.package."*"]
debug = 0
