# TODO

This is Ferrum's short dispatch queue. The authoritative capability ledger and
acceptance criteria remain
[FULL_PARITY_RUST_FIRST.md](active_plans/active/FULL_PARITY_RUST_FIRST.md);
[ROADMAP.md](ROADMAP.md) gives the delivery order. Completed Local Document
Open stabilization and the completed Atkinson molecule-label/glyph-bond
acceptance gate are recorded there and are not TODO work. This queue names only
the next evidence gates and contract selections. Each entry identifies its
decision owner, terminal success condition, and validation record; it does not
duplicate roadmap detail.

## Obtain human and release acceptance

- **Owner: Ferrum maintainer with the Qt interaction/accessibility owner.**
  Complete the `PARITY-M5.A` [Template Catalog](active_plans/decisions/m5_template_catalog_v1.md)
  native walkthrough. Success means a current real dialog has accepted keyboard
  focus, visible focus, accessible names/roles/states, contrast, and recovery
  wording. Validate with a fresh real-dialog capture and a signed maintainer
  decision; automated catalog receipts remain supporting evidence only.
- **Owner: maintainer.** Obtain native visual and accessibility acceptance for
  the current full Ferrum surface, including keyboard-only authoring, focus
  recovery, readable Atkinson labels, and assistive-technology output. Record
  the signed result in
  [native_visual_accessibility_acceptance.md](active_plans/reports/native_visual_accessibility_acceptance.md).
  Success means the clean rebuilt application completes the 13-scene tour with
  accepted contrast, focus, names, roles, states, and recovery behavior. The
  tour and automated/agent review support this decision but do not replace it;
  fixed-corpus glyph/bond geometry is already accepted by its pixel gates.
- **Owner: release maintainer.** Before release readiness, obtain a remote-CI
  receipt for the candidate revision, a release-artifact inventory/installation
  receipt, and a handoff record of repository and process quiescence. Success
  means every named remote gate and produced artifact is accepted for that same
  revision with no unexplained active process or worktree change. Validate from
  immutable CI run and artifact links plus the handoff record; keep local build
  and test results distinct from these external records.

## Advance parity through bounded contracts

- **Owner: parity maintainer and Rust interchange owner.** Select the next
  corpus-backed M1/M2 editing or interchange profile. Success means its decision
  defines the accepted grammar, Rust ownership, typed loss/refusal behavior,
  CLI and Qt routes, and semantic import/export/reimport evidence before code
  is admitted. Validate the decision against the authoritative ledger and its
  named corpus. CDX binary, reaction/page-layout CDXML, namespace variants,
  end-directed wedges, and wider chemistry forms remain separate decisions.
- **Owner: Rust render owner with the Qt presentation owner.** Design the next
  bond-presentation slice renderer-first. Success means finite sink-neutral
  Rust geometry and a closed durable presentation exist before one
  corpus-backed Adder or Dotted import/editor route is admitted. Validate the
  decision with Rust semantic/refusal evidence, a CLI/Qt client plan, and CDML
  persistence plus history/reopen acceptance criteria. Delivered CDXML Wavy,
  Bold, and Dash are not generic presentation-import fallbacks.
- **Owner: parity maintainer with the named Rust and Qt slice owners.** Select
  each next M4, M5, or M6 slice as one independently accepted contract. Success
  means Rust semantics and typed outcomes, the required CLI and Qt client,
  durable mutation proof where applicable, and explicit accessibility evidence
  are named before implementation. Validate each selection by its approved
  decision, corpus, and acceptance record. Keep unselected catalog, reaction,
  peptide, carbohydrate, help, clipboard, output, and exchange scope out of
  the dispatch queue until its boundary is chosen.

## Preserve the architecture boundary

- **Owner: parity maintainer.** Keep historical reference material as read-only
  provenance. Success means every newly admitted workflow has a Ferrum-owned
  Rust semantic contract, PyO3 carries frozen facts, and Qt owns interaction
  and accessibility without a second chemistry or durable-document model.
  Validate this in each slice's design and architecture review.
- **Owner: security/product decision owner.** Start M7 only after separate
  plugin and compound-service decisions define authority, isolation,
  permissions, provenance, failure containment, and offline behavior. Validate
  the approved decisions before implementation.
