# Changelog history

Entries through 2026-08-20 are archived here. For current changes, see [CHANGELOG.md](CHANGELOG.md).

## 2026-08-20
### Decisions and failures
- Corrected the root attached-cyclohexane E2E's offscreen menu boundary without changing product behavior. It opens the real Edit menu, proves the unique, visible, enabled shared attach QAction, and activates its popup row through QTest press/release rather than direct `QAction.trigger()` or unsupported popup-menu `Return` dispatch on offscreen QPA.
- Guarded attached-cyclohexane release with its retained durable anchor. A malformed pending C6 intent without `start_atom_id` now retires the native receipt through the normal cancellation lifecycle, presents one refusal, and cannot commit or mutate the document; the existing generic start-scene rule remains unchanged.
- Fixed a release-handler guard that made a valid Attach Cyclohexane Ring drag return before native commit because this gesture intentionally has no generic `start_scene`. The exception is limited to C6 attachment; existing regular ring releases with a missing start scene still refuse without mutation. The focused Qt coverage now proves one native commit and one revision transition.
- Repaired the root attached-cyclohexane E2E's generated child program: the bridge-call wrapper now has valid nested indentation, and the outer test compiles its exact dedented child source before execution so a child-only syntax error cannot be misreported as a successful parent run.
- Extended the existing attached-cyclohexane root E2E's post-release failure payload without changing its gesture or one-transition assertion. A failure now distinguishes the installed Qt snapshot from the authoritative native-session snapshot, records the real commit-and-refresh outcome or its typed exception, and includes active intent plus typed refusal-recorder facts.
- Added bounded, test-local attached-cyclohexane E2E diagnostics for a cleared drag. Its existing real menu-to-viewport interaction now reports passive focus snapshots, presence-only intent state, begin/preview call counts and caught exception summaries, plus typed refusal facts when no native receipt appears. It changes neither product code nor the drag behavior.
- Corrected the root attached-cyclohexane E2E's real menu-route activation. It now waits for the selected popup to become visible and its target action to be selectable, then proves the checkable action armed after the popup click. Bounded failures record only menu and action state; no direct action trigger or product fallback was added.
- Restored the expected bare `./build.sh` developer command. Native builds now use the builder-owned, profile-scoped managed source-archive cache below `build/`: missing hash-pinned HTTPS archives are atomically provisioned once, verified cache reuse is offline, and explicit sealed/archive overrides remain strict. The default never selects a previous output or a nonexistent checkout `vendor/` cache.
- Kept the root attached-cyclohexane E2E fully unattended without changing Ferrum's interactive modal policy. Its generated offscreen window now records only that instance's typed edit refusal for the intentional ineligible-anchor click, then proves exactly one refusal with unchanged document and settled action state.
- Added failure-only, redacted diagnostics to the existing ineligible-anchor attached-cyclohexane E2E assertion. A refusal mismatch now reports typed request presence facts, before/after snapshot identity facts, and C6 action/intent state without serializing CDML or the input path.
- Closed the attached-cyclohexane implicit-anchor picker test gap without new infrastructure. The focused Qt test now proves a rendered hit takes precedence over the C6-only nearby-projection fallback, and an ambiguous visible Attach Cyclohexane Ring press leaves both the durable start atom and opaque native receipt unset.
- Fixed Attach Cyclohexane Ring targeting for ordinary unlabelled carbon atoms. The action preserves normal rendered-atom hit behavior, then only for that action accepts one unique nearest current projection atom within six viewport pixels; equal, absent, and out-of-range candidates refuse without guessing. Generic picking behavior is unchanged.
- Corrected the focused attached-cyclohexane Qt test teardown. After its successful C6 commit, the test now uses the public tab undo path and proves the saved CDML and clean state before closing, avoiding the intended interactive dirty-close dialog in an offscreen run.
- Added value-safe failure diagnostics to the existing root attached-cyclohexane E2E preview assertion. On a missing native receipt it now reports only action/tool activation and presence-level gesture lifecycle facts; it neither changes product behavior nor adds a harness.
- Corrected the attached-cyclohexane E2E wheel-member preflight regex so it selects the direct native `.so` member before retaining the existing venv path, native-suffix, and SHA-256 provenance checks.
- Strengthened the existing single root attached-cyclohexane E2E without adding a harness or fixture. Its Escape branch now first proves a live opaque native receipt and copied preview, while its ineligible-anchor branch proves attach mode was armed and reaches a terminal unarmed refusal with no document mutation.
- Removed the unused public attached-cyclohexane candidate revision/digest accessor. The typed internal receipt now exposes only its copied paint preview; prepared document state remains opaque until the document session commits or retires the transaction.
- Corrected attached-cyclohexane pointer-move dispatch. The Qt controller now begins the Rust-owned preparation before its generic preview/start-scene guard, which intentionally does not apply to this gesture. The focused offscreen behavior test requires the resulting opaque receipt and paint-only preview after a real pointer move.
- Strengthened WP-C1's focused offline Qt lifecycle proof. The existing regular-ring module now records the real cancellation bridge receipt on tab switch, stale-document, and `_close_tab_at` disposal routes; it also proves one retained receipt survives a refused cancellation and retires exactly once on a direct disposal retry without document mutation. The bridge confinement oracle now scans every Python file beneath the Qt application package, allowing only the controller and this test module.
- Repaired WP-C1 attached-cyclohexane cancellation ownership. A Rust cancellation refusal now retains the opaque receipt in a terminal blocked gesture, retires only the disposable Qt overlay, fences all later mutation, and permits retry through the existing cancellation route. Tab-close and application-close paths now refuse disposal while that receipt remains live. The focused offline Qt module covers cancellation refusal/retry plus tab, stale-document, and close retirement routes; its bridge oracle now requires exactly one use of each permitted cooperative bridge method.
- Aligned WP-B1's attached-cyclohexane bridge with its concrete cross-crate contract. The pending C6 transaction is now re-exported as a documented internal Rust seam for `ferrum-api`, while its implementation module remains rustdoc-hidden and fields remain opaque. The real registered Python module test now proves the pending type and any module-level attachment constructor or service remain absent.
- Corrected WP-B1's Rust authority claim. The extension still does not register `PyPendingAttachedCyclohexaneV1`, and its receipt remains Python-opaque, nonconstructible, and serialization-free. The C6 lifecycle is instead an honest public typed internal document seam required by the current crate dependency direction; opaque fields and document session/fence invariants, rather than impossible cross-crate Rust privacy, protect its transaction semantics. A generic prepared-edit framework and session-host migration remain deferred pending evidence.
- Corrected WP-C1's planned literal cooperative-bridge call-site oracle: it scans only Qt production and Qt test source trees, excluding Rust binding definitions and tests. This makes it a Qt repository-usage check rather than an impossible restriction on the Rust definitions it intentionally exercises.
- Repaired the planned cyclohexane PyO3 opacity proof before implementation: WP-B1 requires omission of the module registration for `PyPendingAttachedCyclohexaneV1`, actual Python receipt opacity, the documented typed Rust transaction seam with opaque state, and a literal offline Qt source/call-site oracle for exactly the four cooperative bridge methods. This remains repository-surface discipline, not a claim of isolation from arbitrary same-process Python.
- Corrected WP-B1's PyO3 authority model for the deferred-ID attached-cyclohexane capability. Its four underscore-prefixed calls are a narrow cooperative internal bridge for Ferrum's trusted same-process Qt controller, not access control against arbitrary Python holding a document session. Rust retains the opaque nonconstructible pending value, C6 candidate/topology, target admission, session/revision/digest/anchor/one-use fencing, atomic commit, and retirement; the bridge returns only copied finite vertices and identity-free outcome facts. It adds no general attachment API, serialized receipt, CDML, identifier, CLI, or Qt surface. Compiled shims and process isolation are explicitly deferred as unjustified for this one gesture.
- Added WP-A2's concrete deferred-ID attached-cyclohexane document capability. It keeps the accepted C6 geometry private, reserves five atom and six bond IDs only in copied sequences, fences prepare and commit by revision/digest/session origin, and applies the complete same- molecule extension in one history transition. No generic attachment or template API was added.
- Corrected the cyclohexane attachment work-package boundary after the atomic-extension review. WP-A1 is now only the private pure C6 geometry and closed anchor-admission probe; WP-A2 owns the concrete `PendingAttachedCyclohexaneV1` capability, copied deferred atom/bond ID sequences, revision/digest/session fencing, atomic history commit, stale/cancel/foreign/replay behavior, and real-document nonmutation proof. The selected discipline reserves five atom IDs and six bond IDs only in copied sequences and installs them with the one committed candidate; sequential commits, live preallocation, and generic attachment frameworks remain rejected.
- Added WP-A1's private attached-cyclohexane document-core hypothesis. It uses the settled shared-anchor C6 topology, admits only closed neutral-carbon anchor facts before any identity or session mutation, and has two inline deterministic pose/admission probes. No public receipt, API, Qt, CLI, fixture, or attachment framework was introduced.
- Reframed cyclohexane attachment WP-A1 after its source-level negative result: the plan now implements the smallest private document-core pose, admission, and candidate hypothesis before exercising its inline probe matrix. This avoids an unowned test geometry duplicate while retaining fixed shared-anchor topology, autonomous proof, and no fixture or framework expansion.
- Corrected the cyclohexane attachment plan's autonomous native-wheel prerequisite to use the builder's implemented `build` subcommand with explicit output and sealed-input roots. The JSON `wheel` result remains the exact-artifact selection mechanism.
- Clarified `HUMAN_GUIDANCE.md` as concise direct preferences only; repository style and test policy remain in their canonical documents.
- Resolved the cyclohexane atom-attachment plan before implementation: Ferrum V1 is strict shared-anchor C6 annulation with five new carbon atoms, six new `n1` bonds, and pre-allocation neutral-carbon capacity refusal. The plan now retains the existing detached-ring action and adds a separately named, keyboard-accessible attach action. Its one root E2E is an autonomous fresh native-wheel plus local offscreen-Qt harness with captured diagnostics; no human or network gate remains.
### Changed
- Corrected the blank-canvas Draw Bond wheel E2E so it proves consecutive blank `New` actions
  preserve visible/history state, preserves sticky Draw Bond arming for the Escape preview, and
  records Escape nonmutation before its saved-CDML and undo/redo evidence.
- Closed the Direct Haworth PyO3 preparation-error mapping: structural SMILES syntax refusals now
  surface as the existing actionable `DirectHaworthInputError`, preserving the complete typed
  domain error match without expanding the unsupported Haworth CLI feature.
- Extended the registered C6 PyO3 bridge test to prove that foreign-commit and replay refusals preserve the complete public session snapshot: CDML, revision, digest, and dirty state.
- Mapped the generic empty molecule-batch session refusal through the existing closed PyO3 document and direct-Haworth error categories without exposing a new Python detail surface.
- Completed and accepted M2a.3a's fixed-target CML/CML2 `new_document` import. The Rust API keeps CML decoding, conversion, and CDML handoff private, measures exact bounded protocol envelopes before commit, and lets the CLI publish its explicit artifact only after a committed result. Evidence: `/private/tmp/ferrum-m2a3a-cml-final-evidence.md`; independent acceptance: `/private/tmp/ferrum-m2a3a-final-acceptance-review.md`. Append-current, durable selectors, live PyO3/Qt import, export/conversion, and broader CML semantics remain deferred; this does not claim full CML, OASA, or BKChem parity.
- Extended the existing generic molecule-batch session-fence test: a batch refused by a foreign session now commits on its owning session as one revision transition containing both prepared records.
- Bound M2a.3a's generic prepared molecule batch to its exact process-local document session before token or generated-ID mutation. The existing offline CML CLI E2E now also proves a textual rejected source stays out of both response channels.
- Corrected M2a.3a batch preparation so discarded CML candidates leave both generated-ID allocation and provisional-token state unchanged. Moved the public import summary DTO into the protocol contract and made the CML operation module and closed CML response types crate-private.
- M2a.3a now keeps CML-specific decode, conversion, and CDML handoff private to the API operation. `ferrum-document` exposes only an atomic generic molecule batch candidate, and the named CML protocol operation measures its final success and refusal envelopes before committing.
- Tightened M2a.3a response admission: the CLI now measures its closed CML refusal envelope through the same bounded path as success, while the existing offline root E2E rejects any refusal stderr or document artifact leakage.
### Additions and New Features
- Added the narrow M2a.3a CML open CLI workflow. `ferrum open INPUT --format cml --output NEW.cdml [--json]` admits one CML source into a new Rust-owned document, measures its bounded canonical summary before exposing CDML, and publishes only the explicit output file. The one offline root E2E writes inline CML to a temporary directory and exercises the real CLI.
- Corrected M2a.2 CML document admission proof and refusal mapping. A compact inline two-record CML test now reaches the document prepare seam and proves a late document-owned identity failure preserves the revision, digest, history, projection, and generated-ID state. Candidate serialization failures now map to the distinct redacted `serialization_failed` refusal rather than `internal_failure`.
- Added M2a.2's in-memory CML document-admission seam. It transforms decoded source coordinates into Ferrum document coordinates before identity allocation, commits every admitted record as one atomic transaction, and maps decoder and document failures to a redacted internal refusal seam. Inline Rust tests prove transformed batch admission and a late refusal leaves the document, history, and projection unchanged.
### Decisions and Failures
- Kept M2a.3a deliberately small. Decoder and document-admission failures now retain their exact closed refusal reason through CML open rather than being coarsened to `invalid_xml`; no CML fixtures, corpus, network path, generic conversion route, selector, live receipt, or Qt action was added.
- Re-scoped the next M2a.3a delivery to one stateless `new_document` CML open operation and one `ferrum open INPUT --format cml --output NEW.cdml [--json]` workflow. Append-current-document, durable selectors, live PyO3 receipts, Qt actions, corpus/fixtures, and generic import machinery remain separate later slices. The canonical envelope is still admitted before document/output publication under the existing 1 MiB bound, and the sole CLI E2E stays offline in root `tests/e2e/`.
- Recorded the KISS decision rule in `docs/HUMAN_GUIDANCE.md`: prefer the smallest durable architecture and proof set, and require a concrete current use before adding machinery.
- Added M2a.1's Rust-owned closed CML1/CML2 tokenizer and bounded source-graph decoder. It admits only the documented simple-molecule profiles, returns redacted typed refusals before document mutation, preserves source-space coordinates for document-owned conversion, and uses compact inline Rust coverage without CML fixtures or external execution.<br>- Corrected M2a.1 CML/CML2 closed-profile admission: CML2 atom and bond children now refuse at the frame boundary, and XML entity decoding consumes each caller-supplied UTF-8 output budget before allocation or append.
- Corrected M2a.0 CML/CML2 repository conformance. Removed the premature committed fixture corpus, manifests, hash-inventory harness, and its chemistry-only test dependencies. M2a.0 now retains compact in-process registry, typed-refusal, DTO, and budget relationship coverage only.
- Added M2a.0's Rust-owned CML/CML2 import foundation: a cross-layer registry, closed request/refusal/budget DTOs, and a private response-budget value reserved for M2a.3's future exact-envelope admission. This phase adds no XML parser, document mutation, canonical response admission, CLI route, PyO3 receipt, or Qt action.
- Corrected the M2a CML/CML2 import contract's response channel. The 8 MiB Rust-private candidate is no longer contradictory with the 1 MiB public JSON envelope: import is explicitly control-plane only, and success returns only bounded summary, provenance, revision/digest, loss facts, and durable molecule selectors. Complete CDML, candidate CDML, source CML, and converted artifacts cannot cross either protocol or CLI result channel. Explicit CLI `--output` remains atomic post-success document publication; non-JSON output is bounded status/summary only. Any artifact-delivery route requires a separately named conversion/export contract.
- Replaced the provisional M2a CML/CML2 design with a closed import-only contract. It now freezes namespace/root/container grammar, XML lexical behavior, CML array-attribute refusal, a y-down CML to y-up Ferrum coordinate transform, every budget/refusal triple, public-selector versus live-receipt ownership, a real cross-layer format registry, fixtures, and proof gates.
- Accepted the M4b Rust SMARTS parity milestone after independent backend, named-CLI, private-live-bridge, and isolated packaged Qt dock evidence. The final GUI receipt covers raw and direct-root selected queries, ownership handoff, result replay/retirement, mutation/reprojection, Save As/reopen, and tab switching. M4b completion does not claim broader OASA/BKChem parity or release readiness; M2a CML/CML2 interchange is the next recommended work.
- Made FerrumNativeMainWindow the sole owner of outgoing SMARTS tab retirement. A real tab switch now clears and hides copied dock state only after one native retirement attempt, unbinds the outgoing callback, and binds the incoming tab without touching its published plan. Failure clears stale UI without retrying native retirement; first-tab activation and document mutation remain non-hiding.
- Corrected first native-tab activation to re-enter the authoritative tab lifecycle only after registration publishes the page mapping. Added a bounded queued Open helper and two-tab SMARTS regression proving outgoing receipt retirement and incoming direct queryability.
- Corrected Qt native-tab activation to retire only the outgoing live SMARTS render plan. A newly admitted tab now retains its already-installed plan, while the outgoing tab's opaque receipt remains invalidated. Added an asynchronous two-tab regression that proves the incoming tab queries immediately without a repair observation.
- Corrected the Qt native-tab test session so receipt-only SMARTS cleanup no longer models full render-plan retirement. The fixture regression now proves receipt clearing permits a same-plan rerun, while full retirement refuses it.
- Made current stale SMARTS delivery retirement fail closed. A failed native receipt-only retirement now raises the typed refresh-required error instead of reporting that the delivery was dropped, while retaining the opaque receipt and unavailable state for safe recovery. The direct-session audit explicitly classifies both private SMARTS retirement entry points.
- Split private live SMARTS retirement into receipt-only query cleanup and full render-plan lifecycle retirement. Clear, rerun, stale delivery, and second Escape now revoke every opaque receipt without unpublishing an unchanged drawing's authoritative plan; mutation, reprojection, deactivation, and disposal still revoke both plan and receipts.
- Corrected SMARTS selected-molecule canvas capture to use Rust's direct-root interaction observation and point-selection boundary. A molecule glyph click now produces the compatible opaque selected-query token without Python atom to molecule inference; focused Qt coverage rejects the structural-child path.
- Made Qt pointer-tool action handoff invoke SMARTS selected-root cancellation and the real command handler through one ordered dispatch slot; added a real Add Atom integration regression.
- Corrected the SMARTS Query action's selected-molecule capture focus policy. Opening the modeless dock now leaves an armed canvas choice focused and active, while a later registered canvas-tool handoff still retires it before its handler takes ownership.
- Corrected SMARTS dock ownership: each Ferrum window now retains the single base-created dock/controller/action, while only the capture owner holds a selected-query token. The dock carries copied readiness through deferred UI work and consumes the token synchronously only at query admission.
- Replaced SMARTS selected-molecule capture's one-time QAction scan with an explicit main-window interaction-action handoff. Current and future pointer-owning tool actions now retire capture before their own handler runs.
- Corrected modeless SMARTS dock closed-error decoding. It now accepts only exact frozen PyO3 category/reason/recovery enum triples, maps every native closed outcome to user-facing recovery guidance, and fails unknown or incomplete outcomes closed as typed unavailability.
- Corrected SMARTS dock lifecycle refresh, document-transition invalidation, and Escape coverage so stale copied results clear immediately after a successful mutation or render-plan replacement without issuing another native retirement.
## Unreleased
- Placed the permanent arrow-authoring workflow E2E in the canonical root E2E lane.
- Added a compiler-derived Rustdoc JSON oracle that rejects retired M4b SMARTS
  renderer authority across the render, document-render, and API public surfaces,
  including hidden re-exports, aliases, and globs.
- Corrected modeless SMARTS dock recovery handling. A false overlay-retirement result now fails closed without claiming success, and selected-molecule readiness codes are translated into visible, accessible recovery guidance.
- Corrected the modeless SMARTS dock action lifetime and tree-result accessibility. The live Chemistry action now remains refreshable, while result groups and rows use Qt-supported accessible item text and descriptions.
- Made the final combined installed-wheel SMARTS E2E hermetic for every Qt child and named CLI request. It now installs the exact sealed bundle into one dedicated `HOME`, clears ambient runtime variables, uses a non-symlink Save As destination, and emits bounded faulthandler diagnostics for a real Save As stall.
- Corrected the sealed installed-wheel SMARTS multi-row E2E retirement baseline. The test now records the required construction-time render fence, proves first installation and successful row replacement add no retirement, and still requires exactly one additional retirement after forced restore failure. Its replay and retired-row assertions now use the sealed live-query boundary's current closed error type and text.
- Made the final installed-wheel SMARTS CLI proof install the matching sealed engine bundle into an isolated temporary application-data root and require the CLI to report it ready before either named query runs.
- Corrected the composed installed-wheel SMARTS E2E harness to provide its copied Qt bridge test explicitly, rather than deriving it from a temporary proof-script path.
- Corrected the private SMARTS selected-target snapshot to retain separate, exact durable protocol selectors and renderer-authored source identities. Stateless selected queries now resolve only durable selectors while opaque live tokens resolve only renderer source identities; neither route falls back across identity domains or exposes either identifier.
- Added a final combined installed-wheel M4b SMARTS lifecycle harness. It records fresh sealed ABI-5 native and Qt wheel provenance, installed-import roots, opaque selected-token authority failures, real multi-row Qt receipt replacement and full-retirement behavior, named raw and durable-selected CLI redaction, and public Save As/Rust reopen/asynchronous GUI reopen evidence.
- Fixed stateless selected-molecule SMARTS queries to resolve the documented durable direct-root selector, rather than the renderer-only authored source ID. The private snapshot now rejects ambiguous direct-root joins before native dispatch. Added protocol and named CLI regressions proving the durable selector succeeds while an authored ID is refused.
- Corrected private live-SMARTS receipt redemption. `show` now validates its opaque issuer, published plan/session fence, row availability, and complete paint before atomically consuming a row. Replayed or foreign receipts return the closed `receipt_unavailable` reason; paint construction failures return a closed typed refusal without consuming the row. Added selected-token multi-row, replay, foreign, and retirement coverage.
- Fixed the private live-SMARTS selected-token identity join. Direct renderer roots and SMARTS targets now join on the same authored source ID while the durable document identity remains confined to document lowering. Added Rust and sealed-wheel PyO3 regressions for a nonzero selected molecule and identity-free result summaries.
- Corrected Qt SMARTS multi-row lifecycle ownership. An explicit rerun now retires the old native run before bridge dispatch; first-row installation is a non-retiring commit, later row replacement retains the live receipt, and an omitted rerun fence fails closed. Added private bridge event-order and sealed-wheel row-zero/row-one/replay sequencing coverage.
- Added the sealed-wheel selected-query SMARTS authority harness, including one row-zero redemption and replay refusal. Its fresh CPython run currently exposes a product blocker: a valid selected molecule is refused as `selected_source_not_molecule` before native dispatch, so the positive and forced-runtime precedence gate is not yet accepted.
- Added an installed-wheel SMARTS selection-token authority gate. It disables the packaged adapter before matching and proves opaque selected-query tokens reject foreign, stale, multiple, and non-molecule selections before native admission, while raw matching reports only the closed unavailable outcome.
- Fixed the private Qt SMARTS overlay swap rollback: if prior paint cannot be restored after candidate attachment fails, the tab now retires the entire native run and clears its visual owner, opaque receipt, and run token together. Added bridge-sequencing coverage for native row reservation before renderer issuance, replay refusal before Qt replacement, and complete normal retirement cleanup.
- Replaced the live SMARTS selected-query input with a dedicated opaque, session-issued capability. Generic render selections retain their authoring display surface but cannot be redeemed as chemistry input; issuer, selection fence, exact-one root, current plan, and direct-molecule membership now fail closed before native runtime admission or selected-SMARTS derivation.
- Added a private Qt SMARTS overlay replacement transaction. A renderer-issued, identity-free item can replace only the current transient visual while the opaque live receipt and run token remain valid for other unconsumed matches. Failed scene attachment restores the prior visual when possible and never locally derives chemistry or geometry.
- Replaced the private live SMARTS selected-molecule dictionary route with an existing session-bound render-root selection capability. Live Rust/PyO3 now validates that selection's origin and fence before privately deriving its query graph. It returns only closed category, reason, and recovery facts for invalid queries, resource bounds, stale/foreign selections, unsupported documents, receipts, and native availability; no query text, CDML, record identifier, or adapter detail crosses the UI boundary.
- Bound the nonshipping SMARTS response-size PyO3 harness to the exact imported extension member and SHA-256 from its configured harness wheel. Added a separately installed, isolated normal-wheel execution that sets the harness limit environment to `1` and proves the shipping extension ignores it.
- Made the nonshipping SMARTS response-size PyO3 harness skip only when its complete provenance configuration is absent. Any partial or malformed configuration remains a hard failure before extension import.
- Added a nonshipping `response-size-e2e-harness` feature for isolated PyO3 response-admission evidence. It can lower only the already shared SMARTS envelope gate in a separately built harness wheel; ordinary native and Qt wheel routes retain the fixed 1 MiB limit and contain no override hook.
- Added a deterministic 1 MiB admission gate for the public JSON envelope of `document.molecule.smarts.query.v1`. Rust protocol, CLI, and PyO3 now share the canonical envelope serializer; oversized raw or selected-query results are replaced before delivery by the closed, correlation-preserving `resource_limit` / `response_size_exceeded` refusal with no partial rows or query text.
- Corrected the Qt M4b post-publication SMARTS lifecycle. A render-plan replacement now retires the old visual/native state exactly once before Rust publishes the new plan, then installs that published observation without clearing it. Focused event-order coverage proves a query receipt survives installation and that the next mutation revokes it before mutation begins.
- Hardened the M4b sealed-wheel SMARTS provenance gate: an explicitly configured invalid wheel root now fails rather than skips, and both the wheel archive and installed `.dylibs` directory must exactly equal the manifest native closure. Focused fixtures reject extra or missing archive and installed native members, nested archive paths, and installed symlink escapes.
- Strengthened the M4b installed-wheel SMARTS gate with immutable wheel and bundle-manifest provenance, closed member validation, exact archive-to- install byte checks, and ABI-5 SMARTS-capability symbol checks. The controlled selected-molecule receipt now traverses the parsed named CLI command route.
- Made Qt live-SMARTS lifecycle retirement fail closed. Native retirement now retains its opaque receipt until success, permanently blocks later mutation, reprojection, or overlay replacement after failure, and prevents tab/window disposal from releasing the recoverable owner. Added focused deactivation, disposal, and normal-cleanup coverage.
- Added an interruption-resilient M4b SMARTS progress checkpoint. It records accepted ABI-5/native/Qt transaction gates, rejected authority designs, current API and Qt integration work, sealed artifact provenance, and the remaining verification sequence without claiming feature completion.
- Replaced the M4b V3 live SMARTS integer receipt ledger with an API-owned, published-plan bridge. One accepted observation now retains graph-position atom identities during document lowering, a private session publication authenticates every atom-local render anchor before native dispatch, and opaque per-session random receipts reserve one display row before all stale fence checks. Public protocol summaries no longer echo document identity or query origin, and the checked-in schema was regenerated from that redacted DTO contract.
- Moved M4b SMARTS target lowering into API-private owned snapshots, removed the renderer/document-render prepared-target proof surface, and replaced public query text echoes with a closed query-origin fact. Added the private `PyDocumentSession` live query/reveal/retirement seam with one-use receipt reservation and identity-free atom bounds.
- Corrected the M4b SMARTS query response boundary before implementation. Retired public `query_display` and every query-text echo in favor of the closed identity-free `raw_smarts` / `selected_molecule` origin fact; JSON/PyO3 regressions must prove both raw and generated SMARTS are absent from summaries, errors, logs, `Display`, and `Debug`.
- Closed four P1 ownership gaps in the active M4b SMARTS live-query plan before implementation. The feature-neutral API snapshot now requires one-pass graph-position-to-`RecordId` lowering shared by CLI/protocol and PyO3; the sole `PyDocumentSession` owns render-plan publication, retirement, and a monotonic generation; and its receipt ledger reserves each row atomically before validation, consuming post-lookup refusals as well as successful issuance. The plan now requires same-snapshot identity fixtures, complete Qt lifecycle publication coverage, and no-geometry replay/refusal proofs.
- Corrected the M4b SMARTS live-query plan: private API/PyO3 ownership beside the sole `PyDocumentSession` replaces the rejected renderer/document-render prepared-target and redemption design. The plan requires observation-derived shared target snapshots, exact plan/anchor joins, one-use identity-free paint instructions, authoritative retirement, and external opacity proofs.
### Architecture Corrections
- Added the modeless, right-docked Ferrum SMARTS Query surface. Its named `Chemistry -> SMARTS Query...` action dispatches raw or Rust-minted selected queries only through the private live-session bridge, presents bounded source-ordered summaries, and paints only finite identity-free bounds through the existing tab retirement transaction.
- Added focused Qt regression coverage for SMARTS selected-root capture handoff: pointer-owning actions registered before or after capture construction must retire capture before their handlers run and cannot issue a selected-query token afterward; the installed non-owning SMARTS Query action preserves the explicit capture mode.
- Corrected the M4b renderer SMARTS target boundary after trust review. Atom anchors are now authenticated from actual composed atom-local render batches by durable `RecordId`, not by a core/projection source-order zip. The opaque target capability no longer exports `MolGraph`; it offers only typed matcher facts and identity-free overlay projection.
- Retired the rejected Patch 3 public PyO3 SMARTS receipt/paint bridge and its API-owned raw matcher-row escape. The stateless SMARTS protocol remains a bounded DTO-only CLI/data path; live reveal is intentionally unavailable until the renderer-owned prepared-target capability can prove the exact graph-index-to-render-anchor correspondence under one renderer plan.
- Corrected the final M4b Patch 3 live-query scope before implementation. Renderer admission now atomically prepares every eligible direct molecule for raw-SMARTS and selected-molecule queries; selection supplies only the private query graph and never narrows traversal. The plan now identifies the renderer-plan replacement owner as the source of a private monotonic plan generation and requires authoritative source-order, 256-target admission before target lowering, prepared-target allocation, or chemistry.
- Removed the superseded Patch 3 API-side live SMARTS bridge contract from the active M4b plan. The renderer-owned prepared-target and overlay-issuer design is now the sole executable route. Live preparation is atomic across every direct molecule in scope: any incomplete same-plan correspondence fails closed as `unsupported_document` and publishes no result, prepared set, receipt, ledger, or overlay. Stateless query remains separately truthful and does not inherit renderer admission.
- Rejected and replaced the initial Patch 3 live SMARTS bridge projection. `ferrum-document-render` now owns the planned opaque prepared-target capability, including the exact native-graph-index to document-record and render-anchor correspondence; the API ledger may retain only target indexes and native positions, and only the renderer may issue an overlay.  The plan explicitly separates Rust/JSON enforceable non-forge and no-leak guarantees from the cooperative same-CPython Qt integration convention, and records the deletion-first reimplementation and evidence gates.
- Added the Qt-side M4b live-document transaction seam. One document tab now owns the sole transient renderer-issued overlay, retirement receipt, and local run token; it retires paint before every intercepted document mutation, observation replacement, tab switch, disposal, and construction cleanup. Stale delivery tokens are strict no-ops and cannot remove newer output.
- Tightened the Qt M4b transaction seam after review: exact durable-molecule naming and linear-form conversion binding methods now enter the same closed pre-retirement proxy inventory, and a failed native receipt retirement is a typed, fail-closed recovery boundary before either mutation or reprojection.
- Added a source-derived Qt mutation-surface oracle. It parses every direct `self._session.method(...)` call under the Ferrum tab package, accepts only an independently audited read-only set, and requires every other current or future call to be protected by the live-document transaction proxy.
- Replaced the M4b Patch 3 cyclic/live-identity proposal with an acyclic contract: `ferrum-api` privately owns correspondence, generation ledger, and receipts; document-render exposes only identity-free paint projection; and Qt must retire before every mutation or reprojection route.
- Reframed the M4b live SMARTS bridge so its document atom identity join, lease, and issuer are crate-private at their authority boundary. Qt now owns visual retirement before its sole Rust plan-replacement transaction.
### Additions and New Features
- Added the renderer-owned M4b SMARTS prepared-target capability. It atomically admits at most 256 direct molecules from one fenced render observation, retains the private native-graph-index to `RecordId` to render-anchor join, and issues identity-free overlays only for the exact session, fence, and renderer-plan generation. Unsupported, excluded, duplicate, foreign, stale, and out-of-range inputs refuse without a partial target set or paint output.
- Added the first Patch 3 SMARTS query delivery seams: a bounded stateless `document.molecule.smarts.query.v1` protocol/CLI route, an installed-wheel private runtime closure, session-owned opaque one-use query receipts, and an identity-free renderer paint projection. Public JSON exposes only source-order match counts and truthful cap status; it contains no runtime, wire, atom key, renderer, geometry, or reveal capability.
- Corrected the active M4b SMARTS bridge contract before Patch 3 code. A one-shot, fenced `ferrum-document` workspace input now supplies typed direct-root and atom identities only to `ferrum-document-render`, which creates RecordId-aligned private render keys against one leased render plan. The obsolete callback/no-renderer-import narrative is removed: the existing `PyDocumentSession` directly invokes its owned renderer while retaining runtime and ledger policy. Its sole retirement-then-plan-replacement transaction synchronously calls the DocumentTab's idempotent visual-retire hook before any fence mutation or plan publication; public JSON remains stateless and carries none of these integration values.
- Revised the active M4b SMARTS live-bridge design before code: immutable renderer-plan leases and opaque aligned atom keys now provide exact same-snapshot query/reveal correspondence; a session-owned ledger makes receipts one-use and all-or-nothing; installed-wheel chemistry is one private runtime closure for protocol and Qt; and the tab retires a visible overlay before every accepted projection replacement, deactivation, rerun, or disposal.
- Added an environment-gated sealed ABI-5 native SMARTS integration test covering matches, no matches, bounded truncation, and closed invalid-query failure behavior.<br>- Replaced the retired `chemistry-sys` metadata-only check with an exact, compiler-derived `ferrum-chemistry` public-facade oracle. Nightly Rustdoc JSON's crate-root item tree, including `#[doc(hidden)]` items, is checked against the approved typed facade; adversarial macro, `include!`d type, and reexport fixtures prove that raw adapter authority cannot return unnoticed.
- Sealed the native chemistry adapter's direct Rust error boundary. Dynamic loader, ABI, capability, symbol, foreign-buffer, and non-allocation native failures now map exhaustively to one detail-free `NativeBoundary` recovery message; allocation exhaustion and unavailable named operations retain their existing typed chemistry errors. Added a hostile direct-library regression test and external consumer checks that retain typed-engine compilation while refusing direct raw-adapter module, foreign-buffer, and ABI/wire imports.
- Tightened the M4b SMARTS-query plan before implementation with a dedicated closed `ChemistryError::SmartsMatchUnavailable` branch. Every new SMARTS loader, ABI, capability, call, FQM1 decoder, and native-rejection failure must be detail-free and cannot use legacy `NativeBoundary` or `NativeRejected` strings. Direct-library hostile fixtures now require no absolute loader path or adapter/FCQ1/FQM1/native-detail token in public error fields, `Display`, or `Debug`; protocol, CLI, and PyO3 keep their broader redaction gates. This M4b promise is intentionally scoped to `smarts_match`; legacy general-chemistry error redaction remains later work.
- Corrected the M4b SMARTS-query ownership plan before Patch 2 implementation. The existing public `NativeChemEngine::load` explicit-adapter boundary remains a typed general-chemistry surface; only chemistry-sys raw dynamic loading, foreign buffers, and ABI/wire authority move into private `native_engine::adapter_boundary`. M4b's sealed runtime now applies precisely to document-query protocol, CLI, PyO3, and live Qt delivery, while a later architectural milestone may consolidate whole-program runtime ownership.
- Refined the active M4b SMARTS-query plan before Patch 2 implementation. Dynamic adapter loading, foreign-buffer release, and ABI/capability admission now move first into private `ferrum_chemistry::native_engine::adapter_boundary`; the public sys raw facade is removed (or reduced to generated constants). Private FCQ1/FQM1 then feeds only the typed `ChemEngine::smarts_match` operation. Required proof now includes direct sys external-surface refusal and owned typed-result nonleakage, before protocol, CLI, PyO3, or Qt work.
- Advanced the sealed native adapter to ABI-5 with a capability-gated opaque `SMARTS_MATCH` transport symbol. The loader accepts it only when the ABI and advertised bit agree, rejects old ABI-4 and unknown-capability adapters, and retains pinned `RDKitSubstructMatch` through CMake and wheel closure audits. FCQ1/FQM1 matching, public query APIs, CLI/PyO3 delivery, and Qt remain intentionally out of this plumbing-only patch.
- Corrected the active M4b SMARTS-query plan's 64-query-atom admission boundary before implementation. Rust and C++ retain raw-byte preflight, but the pinned native `SmartsToMol` parser now counts full-profile parsed query atoms and returns the closed `INVALID_REQUEST` / `query_atom_limit_exceeded` outcome before matching, row allocation, or receipt issuance. Rust, protocol, and Qt map only that safe closed reason; the evidence gate includes a parser-accepted 65-atom fixture.
- Corrected the active M4b SMARTS-query plan again before implementation. The public query is now explicitly stateless CLI/JSON data only; Qt match reveal is the non-JSON API-binding `LiveDocumentSmartsQueryBridgeV1`, constructed only from one live `DocumentTab` Rust session and using non-serializable, non-redeemable typed receipts. The plan now rejects identical-CDML cross-tab redemption, freezes selected-root lowering through the trusted pinned `molecule_to_smarts` route, and defines public caps, response budget, successful cap facts, and resource-refusal boundaries.
- Corrected the active M4b SMARTS-query plan before implementation. It now fixes ABI-5 FQM1 status/detail legality, accepts the full pinned-RDKit `SmartsToMol` profile, specifies source-order two-level cap accounting and truthful tagged completeness, freezes the durable selected-molecule request, and defines the fenced Qt-only reveal-capability bridge with hostile-detail redaction and lifecycle evidence gates.
- Added the active M4b SMARTS-query plan, defining the ABI-5 FCQ1/FQM1 capability, Rust-owned public operation boundary, JSON-only Qt dock, bounded result and cancellation semantics, and required evidence gates.
### Behavior or Interface Changes
- Added the typed, bounded `ChemEngine::smarts_match` general-chemistry seam. Its public options require a positive capped row limit and its result contains only owned query-ordered atom positions in the caller's `MolGraph` plus a truncation fact. ABI-5 FCQ1/FQM1 bytes, native indexes, loader state, foreign buffers, paths, pointers, and document identity remain private. Every SMARTS loader, capability, call, decoder, and native-rejection failure now maps to the closed, detail-free `SmartsMatchUnavailable` branch rather than legacy native-boundary errors.
- Replaced molecule-report's independently nullable aggregate fields with one required tagged `aggregate` outcome. Its `complete` branch carries the full composition DTO, while its closed `omitted` branch carries exactly `fewer_than_two_selected` or `incomplete_record_composition`; contradictory or arbitrary-string aggregate states now fail Rust and JSON decoding.
- Replaced molecule-report formula-only protocol fields with one explicit all-or-none composition DTO per record and aggregate. The public CLI and PyO3 JSON now include finite formula, net formal charge, average molecular weight and monoisotopic mass in Da, plus canonical isotope-aware element counts, contributions, and mass percentages; unavailable compositions remain `null` with the existing closed findings or aggregate omission reason.
- Moved the PyO3 extension implementation into `ferrum-api`, making the package's `cdylib` the only code that can capture its initialized wheel path. Runtime-aware protocol execution and adapter loading are now crate-private, use one immutable initialized-module-derived sibling path, and cannot be reached by a Rust dependency even when it enables the Python binding feature. The Python package now retains only Maturin metadata and binding tests.
### Developer Tests and Notes
- The default and `python-binding` external-consumer builds now prove that a valid fenced molecule report reaches only the typed no-runtime refusal while private binding, runtime, execution, report-core, and chemistry imports do not compile. The canonical native-wheel launcher was attempted against both retained sealed inputs and refused their stale header-tree evidence before compilation; a regenerated validated input root is required for wheel E2E.
### Fixes and Maintenance
- Corrected direct-root move ownership at two opaque boundaries. Shift point selection now carries its Rust-revalidated prior selection into the Rust toggle query, and an exact zero-distance view-grid gesture returns a zero translation rather than snapping an off-lattice root on click.
- Tightened the M2a.3a CML create-only boundary. Document-owned preparation now rejects every nonempty session before candidate construction, public summaries no longer expose source identifiers, and the named protocol route measures its exact standard envelope before commit. Added the named CML command to the existing single offline root E2E; it proves success and redacted refusal behavior without fixtures, network access, or a second E2E.
- Corrected M2a.3a CML import to expose only the fixed new-document operation `document.molecule.interchange.import.v1`. The public result is a bounded summary or typed refusal; append/snapshot/placement fields and public CDML handoff were removed. The sole temporary-input E2E now proves success and a refusal leaves no output file.
- Corrected SMARTS dock tab-switch ordering so the outgoing dock clears and hides before it binds the already-current incoming tab.  This prevents a hide callback from touching an incoming live query plan.<br>- Enforced the caller-selected SMARTS match cap at the private FQM1 decoder boundary, so a divergent native response cannot surface more typed match rows than requested. The public option limit and synthetic ABI-5 capability fixture now derive from the header-generated private adapter contract.
- Retired the `ferrum-chemistry-sys` workspace package instead of preserving a constants-only raw facade. `ferrum-chemistry` now generates the loader's ABI status and capability constants directly from the native header inside its private adapter contract; Cargo metadata and an isolated consumer proof verify that no raw package or dependency remains while typed chemistry behavior stays available.
- Kept ABI-5 SMARTS transport admission private to the native loader. The adapter still rejects an advertised capability without its exact symbol and accepts a matching pair, but exposes neither an availability predicate nor a generic raw-byte SMARTS call before the FCQ1/FQM1 protocol core exists.
- Corrected Molecule Report copy output to include the Rust-issued tagged aggregate outcome exactly once.
- Corrected the Qt Molecule Report projection to consume the current nested Rust composition and tagged aggregate DTOs. The modeless dialog now presents finite masses, net charge, isotope-aware contributions, and complete or omitted aggregate outcomes without Python-side chemistry reconstruction.
- Bound each modeless Molecule Report dialog to the exact tab that produced its receipt. `Run again` now recaptures only that active source tab, becomes unavailable while another tab is active, and is terminally retired before its source tab is disposed. Focused Qt and installed-wheel E2E paths cover tab switching and source closure without document mutation or atom reveal.
- Closed direct PyO3 chemistry adapter and ABI failures behind the same `chemistry_unavailable` refusal used for installed-wheel loader failures. Native diagnostics, loader paths, adapter labels, and `dlopen` text can no longer reach direct Python exception text or public fields; Ferrum-owned semantic chemistry refusals retain their actionable details.
- Redacted trusted chemistry-runtime loader and adapter failures from every `chemistry.convert` and `document.generate_coordinates` JSON and PyO3 protocol response. Both operations now retain request correlation while returning the fixed `chemistry_unavailable` category and recovery message; semantic conversion and coordinate refusals remain user-actionable.
- Redacted installed-wheel native loader diagnostics from Python chemistry exceptions while retaining direct chemistry operation semantics.
- Unified the private PyO3 direct-chemistry and protocol runtime under the single immutable wheel runtime captured at extension initialization. The binding no longer reads `__file__` twice or keeps a second extension-path cache; post-import module metadata changes cannot redirect runtime lookup. Direct chemistry failures now publish only closed redacted `category` and `recovery` facts, never an adapter filesystem path.
- Hardened `build.sh native` output admission: the CLI build now uses Cargo's locked dependency graph, and the wrapper creates only the repository-owned physical `output_native_wheel/` directory before allocating an invocation root. Symlinked and non-directory parents refuse before any output or native-builder invocation.
### Developer Tests and Notes
- Added `tests/e2e/e2e_build_sh_native_wrapper.sh`, a controlled-command wrapper proof covering selector admission, both exact native-builder selector vectors, output-parent refusal, exact native receipt validation, and `all`/`cli`/`native`/`qt` instruction behavior without compiling native sources.
- Replaced root `build.sh native` bare-Maturin packaging with an explicit sealed-input or source-archive wrapper around the authoritative native-wheel builder. Each native invocation now retains a fresh `output_native_wheel/` root, validates the builder's one-line artifact record, produces the matching engine bundle, and prints exact CLI-engine and GUI install/run paths.
- Sealed molecule-report chemistry delivery inside `ferrum-api`: external Rust consumers retain only the JSON protocol executor, while CLI and the installed PyO3 extension resolve their own trusted bundle without exporting a runtime capability, adapter path constructor, or generic runtime-aware executor.
### Additions and New Features
- Replaced the competing Molecule Information and Bond Capacity Qt action paths with one modeless `Chemistry -> Molecule Report...` client. The client sends only the frozen public `document.molecule.report.v1` JSON request on a detached worker, presents ordered Rust receipt facts and bounded findings, supports delivery cancellation and explicit Run again, and retains snapshot-stale warnings without document or selection mutation.
- Moved molecule-report graph preparation and native execution from `ferrum-document` into a private `ferrum-api` protocol enclave. Added the schema-owned `document.molecule.report.v1` JSON operation and named CLI route; requests and responses carry only fenced CDML input plus durable report facts, never prepared reports, chemistry graphs, engines, adapter paths, or native diagnostics. The document molecule-information operation now owns its own private corroborating resolver rather than depending on report machinery.
- Sealed the document molecule-report graph preparation and execution seam inside its owning module. Public report receipts and errors now carry only closed, redacted facts; resource exhaustion refuses the complete request, aggregate omission is an explicit bounded document-level fact, and selector byte length is admitted before document resolution.
- Added the Rust-owned bounded molecule-report core. It resolves selected direct-root molecules once from an authenticated revision/digest observation, preserves useful source facts when a composition facet is unsupported, evaluates neutral bond capacity as a record facet, and emits all-or-none combined composition without exposing CDML or native chemistry graphs.
- Added closed, bounded molecule diagnostic facts in `ferrum-domain`, including safe code, severity, recovery, and location vocabularies plus a 256-byte detail limit. The core currently remains below the protocol/PyO3/CLI and Qt delivery layers.
- Refined the modeless Reaction Inspector lifecycle boundary: inspector-owned role and deletion dialogs remain live only while they are the active application modal, while a real peer-window focus transition retires the inspector without mutating the document.
- Corrected the installed-wheel Reaction Inspector E2E delete-dialog driver to resolve the live Qt modal dialog rather than an unreliable global-widget enumeration. It continues to click the real Ferrum delete or cancel control, so the test no longer strands the dialog's nested event loop.
- Made Ferrum's reaction-definition deletion dialog a stable, accessible public Qt surface with an explicit Cancel default. Inspector hide and close events now retire its disposable state even while an owned modal is visible, and a stale role projection recovers through the typed membership-changed route rather than exposing a `KeyError`.
- Corrected the isolated Reaction Inspector E2E's temporary publication and teardown paths to use `/private/tmp`, a directory accepted by Rust's atomic save boundary on macOS.
- Corrected Reaction Inspector lifecycle truthfulness. Rust-accepted role patches, definition deletes, and aggregate nudges now clear disposable selection, retry the authoritative projection, and report recovery rather than calling the accepted mutation a refusal. Role editing preserves each existing durable role ordinal rather than reordering it by global root source order; hostile document labels are escaped before detail-panel HTML rendering; and Inspector-owned modal dialogs no longer trigger false window-deactivation cancellation.
- Added the installed-wheel public Reaction Inspector E2E. It drives the shared Edit/Ribbon action, cancels and accepts the real role-editor and deletion dialogs, proves definition-only deletion retains roots and Undo restores references, and covers aggregate nudge plus Rust and asynchronous native GUI reopen after the public Save As route.
- Added the modeless `Reaction Inspector` to the public Ferrum Edit menu and Authoring Ribbon. It renders only Rust-issued reaction lists, diagnostics, member bounds, and role order; it highlights exact member roots, atomically replaces complete role membership, confirms definition-only deletion, and translates every member through opaque Rust aggregate nudge receipts. Inspection state retires on cancellation, tab/window lifecycle changes, stale refusals, and accepted mutations before reacquiring authoritative observations.
- Published `reaction.translate.v1` in the generated Ferrum operation schema and named CLI command route. Added static-schema freshness, protocol/CLI, PyO3, and isolated-wheel proofs for free and hex-grid aggregate translation, reference retention, undo, and sealed refusals.
- Added opaque renderer-preflighted `reaction.translate.v1` ownership. A strict `ReactionSelectionV1` now moves all and only direct reaction members as one snapped Rust gesture, detached candidate, renderer proof, and single history transaction. The API, PyO3 binding, and CLI protocol retain the selection, preview, receipt, membership digest, and full candidate as non-forgeable implementation details while preserving reaction definition references.
- Moved renderer interaction and opaque reaction-selection ownership from the transport-facing API crate into `ferrum-document-render`. Reaction membership replacement and definition deletion can now begin only from the exact renderer-issued `ReactionSelectionV1`; the former forgeable fence/reaction-ID/membership-digest entry points no longer exist. The API crate reexports the interaction facade without owning its authority.
- Added closed lifecycle refusals for missing reactions, legacy display-only definitions, changed membership, and renderer exclusions, with matching Rust, CLI-protocol, and PyO3 recovery facts. Failed foreign commits retain the prepared receipt for the owning session's retry; successful commits consume it exactly once.
- Published `reaction.patch-membership.v1` and `reaction.delete-definition.v1` as named Ferrum CLI commands, matching their existing typed protocol routes.
- Added renderer-preflighted Rust reaction lifecycle stage 1. `reaction.patch-membership.v1` atomically replaces every role list for one strict selected reaction, and `reaction.delete-definition.v1` removes only that reaction record while preserving its members. Both routes use a frozen membership digest, session fence, private complete candidate, renderer exclusion check, and one-use commit receipt; aggregate member deletion remains deliberately deferred.
- Exposed the lifecycle operations through the typed CLI protocol and opaque PyO3 gesture/receipt values. Neither surface accepts reaction XML, a DOM node, a root-set capability, or a generic complete-CDML transaction as a reaction mutation route.
- Added immutable Rust-owned reaction lifecycle observations and opaque selection capabilities. Direct CDML reaction definitions retain source-ordered role members and compatibility diagnostics; rendered bounds and selection authority remain bound to one document session and snapshot fence.
- Exposed reaction observation union bounds and closed compatibility diagnostics through frozen PyO3 facts. Python now receives Rust-issued reason, recovery, and selector-source values without XML, DOM, candidate, render-plan, or mutable-root authority.
- Added closed stateless `reaction.list.v1`, `reaction.observe.v1`, and `reaction.select.v1` protocol and CLI routes. They return only frozen durable facts and re-establish the opaque selection inside each fenced request rather than serializing a capability between processes.

### Earlier entries for 2026-08-21

- Replaced shared-window and SMARTS-dock inspection of a tab's private disposal field with the public `FerrumNativeDocumentTab.is_disposed` lifecycle contract.
- Relocated the native wheel builder library from `packages/ferrum-rust/tools/` to `packages/ferrum-rust/wheel_lib/`, with `native_wheel_builder.py` as its executable adapter.
- Retired seven uncollected wheel and installed-site E2E scripts from the Qt package. `build.sh` now has one local-build contract, and durable local behavior remains covered by the collected Rust, binding, Qt, and repository E2E suites rather than obsolete package-install probes.
