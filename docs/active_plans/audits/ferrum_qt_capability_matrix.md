# Ferrum-Qt capability matrix

## Purpose and status

This is the finite closure ledger for M19. It records the user-facing Ferrum-Qt
capabilities present before the namespace rename and the replacement work that must
close each capability. It is not an assertion that OASA has been removed.

M1b is complete: the installed `ferrum-qt` command starts the renamed application
offscreen, opens `tests/e2e/corpus/authored_document_forms.cdml` through the existing
Qt/OASA-backed native CDML route, writes the fixed controlled-startup receipt, and
exits cleanly without a traceback. This proves M1b rename/start/open behavior, not
Rust-backend adoption or worker-format completion. The focused lifecycle and CLI suite
reports 21 passing tests; the package suite reports 918 passed and 1 skipped. M19
closes only when every row classified supported has its named validation artifact
passing. Rows marked unsupported path need a recorded decision to reproduce, fix, or
drop before closure.

## Scope and method

The inventory was derived from the menu registry at
`packages/ferrum-chem-qt.app/ferrum_qt/resources/menus.yaml`, the 13 registrar
modules named by `actions/registrar_manifest.py`, `actions/`, `modes/`, `dialogs/`,
and `io/export.py`. It groups actions only when they have the same user workflow,
current owner, OASA boundary, replacement workstream, and exit gate.

The pre-rename source-tree measurement covers 140 production Python files and 118 package test
files. There are 64 direct OASA import statements in 16 production files; there are
445 total `oasa` tokens in 18 production files, including uses, annotations,
messages, comments, and imports. The production tree has zero direct Tk/Tkinter
imports. The 29 Tk/Tcl text hits are historical comparison comments rather than a
runtime dependency. These are source measurements, not counts inferred from the
gitignored historical checkout.

`direct import` means an executable `import oasa...` or `from oasa ...` statement.
`use` means an executable OASA type, function, or object access after import.
`string/history` means labels, messages, comments, or provenance that may name an
old system without loading it. M1e begins with an empty capability list and only
forbids OASA in a row after that row's Ferrum replacement has landed.

## Identity and persistence decision

M1b changes the product-facing package namespace from `bkchem_qt` to `ferrum_qt`,
the console-script target, application display strings, and
package-data metadata. It does not silently migrate user persistence identifiers.

| Decision | M1b disposition | Reason and later gate |
| --- | --- | --- |
| QSettings organization/application names | Retain `BKChem` / `BKChem-Qt` | These are durable storage identifiers, not display branding. Retention preserves existing preferences without an incomplete platform-specific migration. |
| User template directory `~/.bkchem/templates` | Retain | Existing user templates remain discoverable. A new Ferrum location requires a migration decision and E2E coverage. |
| Clipboard ownership marker and smoke receipt schema | Retain existing protocol values | These compatibility identifiers may be consumed by running integrations or existing smoke tooling; Ferrum-named Python constants keep product-facing code current. |
| Product display name, About box, CLI text, repository URL | Change in M1b | These are product identity, not persisted document state. The About box keeps a BKChem lineage acknowledgement while correcting license, repository, and backend claims. |
| CDML labels and provenance references | Retain where historical | `BKChem CDML`, CDML document content, oracle configuration, and provenance describe lineage or a file format; they are not evidence of a live package namespace. |

## Capability ledger

| ID | Capability | Current owning files | Current behavior / classification | OASA / Tk coupling | Replacement milestone / workstream | Present validation artifact | Required exit validation | Remaining decision or risk |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| FQ-001 | Start application and create tabs | `app.py`, `cli.py`, `main_window.py`, `models/document_session.py` | supported | OASA use through the session; Tk none | M1b WS-F, then M8a WS-D and M16 WS-F | `tests/test_application_shutdown.py`, `tests/test_cli_smoke_exit.py`, `tests/test_qt_gui_smoke.py` | M1b evidence passed: installed `ferrum-qt --version`, controlled offscreen start, and an existing Qt/OASA-backed native CDML tab from `authored_document_forms.cdml`; it does not prove Rust-backend adoption or worker-format completion | Rename no longer depends on an old namespace or hidden registrar string. |
| FQ-002 | Native CDML open, save, Save As, close, and recovery export | `actions/file_actions.py`, `main_window.py`, `models/document_session.py`, `io/cdml_document_io.py` | supported | direct import and dominant OASA session use | M6-M10 WS-D, M8a WS-D, M16 WS-F | `tests/test_cdml_document_sessions.py`, `tests/test_document_sessions.py`, `tests/test_snapshot_export_result_boundary.py` | Thin workflow: open one-molecule CDML, render Ferrum operations, save without losing persistent objects | Current OASA session owns revisions, baseline, and serialization; M8a is deliberately narrower than full M16 adoption. |
| FQ-003 | Native CDML typed projection and opaque preservation | `io/cdml_document_io.py`, `io/cdml_candidate.py`, `io/cdml_fragment_builder.py`, `io/cdml_inspection.py`, canvas projection files | supported | direct import and use of CDML/XML observations; Tk none | M6-M8 WS-D, M10 WS-G | `tests/test_backend_projection_replacement.py`, `tests/test_cdml_inspection_boundary.py` | Preservation gate against the M1d coverage inventory and corpus | Do not claim lexical fidelity until the M6 experiment selects structural or raw-slice retention. |
| FQ-004 | Import chemistry files | `actions/file_actions.py`, `io/import_capabilities.py`, `io/format_bridge.py`, `bridge/worker.py` | supported: CDML, MOL, SDF, SMILES, CDXML, and CML routes are advertised | direct codec-registry import and use | M5 WS-B; M18 WS-F for batch/CLI surface | `tests/test_import_capabilities.py`, `tests/test_format_bridge.py`, `tests/test_smiles_import_worker.py` | One E2E round trip for each supported codec, with no new differential divergence | M1b validates only the existing Qt/OASA-backed native CDML route, not Rust-backend adoption or worker-format completion. Worker-routed non-CDML import completion remains a replacement risk. |
| FQ-005 | Export molecule/document codec ledger | `actions/file_actions.py`, `io/format_bridge.py`, `bridge/oasa_bridge.py`, `models/document_session.py` | `.mol` and `.cdml`: supported by the present registry test. `.sdf`, `.smi`, `.cdxml`, `.cdsvg`, and `.inchi`: conditionally exposed only when the current OASA codec reports `writes_files`; no dedicated current validation establishes each. | direct codec-registry import and use | M5 WS-B for `.mol`, `.sdf`, `.smi`, `.cdxml`, and `.inchi`; M6-M8/M16 WS-D/WS-F for `.cdml`; M13 WS-C plus M16 WS-F for `.cdsvg`; M18 WS-F supplies retained CLI routes | `tests/test_format_bridge.py`, `tests/test_smiles_export_backend_authority.py` | `.mol`: codec differential plus CLI E2E. `.cdml`: preservation/thin-workflow save gate. `.sdf`, `.smi`, `.cdxml`, `.cdsvg`, `.inchi`: each gets a codec/render E2E if retained, otherwise an explicit unsupported-path/drop decision. | The finite source map is exactly `.mol`, `.sdf`, `.smi`, `.cdml`, `.cdxml`, `.cdsvg`, `.inchi`; do not advertise a map entry merely because a historical codec exists. Qt SVG/PNG/PDF output remains FQ-006. |
| FQ-006 | Export SVG, PNG, and PDF from a snapshot | `actions/file_actions.py`, `io/export.py`, `io/snapshot_render.py`, `io/render_plan.py` | supported | direct OASA render import/use; Qt painting is also authoritative for legacy scene export | M12-M13 WS-C, M8a WS-D, M16 WS-F | `tests/test_export_render_plan.py`, `tests/test_snapshot_render.py`, `tests/test_copy_as_svg_backend_authority.py` | Render-operation snapshot test plus artifact E2E and visual acceptance at M13 | Keep snapshot revision/error boundaries; do not turn a failed backend capture into a stale scene export. |
| FQ-007 | Draw atoms, bonds, rings, and bond presentation | `modes/atom_mode.py`, `modes/draw_mode.py`, `canvas/items/atom_item.py`, `canvas/items/bond_item.py`, `bridge/oasa_bridge.py` | supported | direct import/use for atom, bond, molecule, periodic-table, and render facts | M2-M3 WS-A, M4d WS-B, M12 WS-C | `tests/test_draw_backend_authority.py`, `tests/test_atom_chemistry_backend_authority.py`, `tests/test_bond_presentation.py` | Core/parity tests, then in-app generated-coordinate slice | Separate core molecule truth from Qt item editing and from rendering operations. |
| FQ-007a | Persistently edit or delete atoms and bonds | `actions/context_menu.py`, `actions/property_editing.py`, `dialogs/atom_dialog.py`, `dialogs/bond_dialog.py`, `widgets/property_dock.py`, `models/document_session.py` | supported: context-menu Delete; set atom element; atom property patch; bond order, type, style, and property patch from dialogs or dock | direct OASA document-session use; Tk none | M2 WS-A for atom/bond meaning and identifiers; M8 WS-D for typed mutation records; M16 WS-F for complete Qt adoption | `tests/test_structure_delete_backend_authority.py`, `tests/test_atom_properties_backend_authority.py`, `tests/test_bond_properties_backend_authority.py`, `tests/test_qt_backend_session_adapter.py`, `tests/test_property_dock_undo.py` | E2E scenarios delete an atom and a bond, change an element, patch atom properties, and change bond order/type/style; each must preserve durable ID, revision conflict behavior, undo/retry behavior, and reproject the accepted snapshot once. | This is not covered by drawing: local Qt undo must never become a fallback for a synchronized Ferrum session. |
| FQ-008 | Read and generate SMILES and read InChI | `actions/chemistry_actions.py`, `bridge/oasa_bridge.py`, `bridge/worker.py`, `bridge/chemistry_preparation.py` | supported | direct import/use of SMILES, InChI, and coordinate generation | M4b-M5 WS-B, M4d WS-F | `tests/test_smiles_export_backend_authority.py`, `tests/test_text_import_workers.py` | Differential codec report and Ferrum-Qt text-import scenario | InChI capability requires the native adapter/RDKit route; do not reproduce an approximate fallback. |
| FQ-009 | Generate coordinates and insert parsed molecules | `bridge/worker.py`, `bridge/chemistry_preparation.py`, `bridge/insertion_placement.py`, `models/document_session.py` | supported | direct coordinate and insertion-geometry import/use | M4b-M4d WS-B, M11 WS-C, M8a WS-D | `tests/test_qt_backend_session_adapter.py`, `tests/test_template_placement_scale_parity.py` | M4d UI slice parses SMILES and displays Ferrum-generated coordinates | Placement includes document-session revision semantics, not only x/y calculation. |
| FQ-010 | Chemistry information, check, oxidation, groups, fragments, names, and linear forms | `actions/chemistry_actions.py`, `models/document_session.py`, `bridge/oasa_bridge.py` | supported | direct document and molecular-model import/use | M2-M5 WS-A/WS-B, M8 WS-D, M16 WS-F | `tests/test_atom_element_backend_authority.py`, `tests/test_fragment_backend_authority.py`, `tests/test_linear_form_backend_authority.py` | One scenario per supported action, mapped to the model/document or chemistry parity report | This row intentionally contains several menu actions because their common boundary is a typed document mutation; split in implementation if their Ferrum contracts diverge. |
| FQ-011 | Repair geometry, align, rotate, translate, and stack transforms | `actions/align_actions.py`, `actions/repair_actions.py`, `actions/top_level_transform_actions.py`, `modes/edit_mode.py`, `models/document_session.py`, `bridge/display_geometry.py` | supported | direct document-session use; display geometry directly imports OASA helpers | M11 WS-C, M16 WS-F | `tests/test_repair_actions.py`, `tests/test_edit_mode_atom_translate.py`, `tests/test_top_level_transform_backend_authority.py` | Geometry parity/tolerance report plus document-session action E2E | M11 must derive tolerances; known historical geometry behavior is not automatically a requirement. |
| FQ-012 | Insert Haworth sugars | `actions/haworth_actions.py`, `bridge/chemistry_preparation.py` | supported: pyranose and furanose | direct Haworth/sugar-code import and use | M14 WS-E | `tests/test_haworth_insert.py` | Haworth output regression/parity test and in-app insert scenario | Keep direct glycosidic and verified sucrose separate because their contracts are not interchangeable. |
| FQ-013 | Insert verified sucrose and direct glycosidic Haworth structures | `actions/haworth_actions.py`, `bridge/chemistry_preparation.py` | supported | direct Haworth import/use | M14 WS-E | `tests/test_haworth_insert.py` | Dedicated M14 scenario for each action | The current names describe distinct biochemical workflows and must not be hidden by a generic sugar row. |
| FQ-014 | Insert biomolecule templates and peptide sequences | `actions/insert_actions.py`, `actions/chemistry_actions.py`, `modes/biotemplate_mode.py`, `bridge/worker.py`, `models/document_session.py` | supported | direct biomolecule-template, template-placement, and peptide utility import/use | M15 WS-E with M8a WS-D | `tests/test_biotemplate_backend_authority.py`, `tests/test_template_backend_authority.py` | Bio-template and peptide insertion E2E through the new session boundary | Generic placement and peptide construction have distinct owners; retain sub-scenarios in M19. |
| FQ-015 | Look up and insert PubChem compounds | `actions/pubchem_actions.py`, `bridge/chemistry_preparation.py`, `models/document_session.py` | supported, network-dependent | direct OASA PubChem/HTTP import and use; no Tk | Unassigned by v3; decision required before M19 | `tests/test_pubchem_lookup.py` | Explicitly classify as supported, known defect, or unsupported path; if supported, add a controlled transport contract and E2E | This is not an offline chemistry codec. It must not disappear inside M5 or bypass rate/error policy. |
| FQ-016 | User templates: save, scan, refresh, inspect, and insert | `actions/file_actions.py`, `modes/user_template_mode.py`, `io/user_template_catalog.py`, `bridge/user_template_inspection.py`, `models/document_session.py` | supported | direct OASA CDML-document use in inspection/session path | M6-M8 WS-D, M8a WS-D, M16 WS-F | `tests/test_user_template_catalog.py`, `tests/test_user_template_backend_authority.py`, `tests/test_user_template_mode.py` | Template directory compatibility decision plus load/save/insert E2E | Retain `~/.bkchem/templates` in M1b unless a migration is designed and tested. |
| FQ-017 | Edit presentation objects: text, rich text, arrows, plus, wavy, brackets, vectors, and paper | `modes/`, `dialogs/`, `actions/object_actions.py`, `models/document_session.py`, canvas item files | supported | document-session direct import/use; Tk none | M6-M8 WS-D, M12 WS-C, M16 WS-F | `tests/test_rich_text_qt_authority.py`, `tests/test_arrow_backend_authority.py`, `tests/test_plus_backend_authority.py`, `tests/test_wavy_backend_authority.py`, `tests/test_paper_properties.py` | Typed/opaque preservation test and render-operation test for each object family | Do not reduce this to molecule-only CDML; these persistent objects define the M8a save acceptance. Atom-attached marks and numbering have their own durable row. |
| FQ-017a | Persist atom numbering and chemical marks | `modes/misc_mode.py`, `modes/mark_mode.py`, `modes/edit_mode.py`, `models/document_session.py`, `canvas/items/mark_item.py` | supported: assign sequential atom numbers, clear numbers, add/remove radical, biradical, electron pair, dotted electron pair, circled plus, circled minus, and pz orbital marks; Delete removes one selected durable mark | direct OASA document-session use; Tk none | M2 WS-A for atom identity; M8 WS-D for typed mark/number records; M16 WS-F for Qt adoption | `tests/test_persistent_atom_numbering.py`, `tests/test_mark_backend_authority.py`, `tests/test_atom_mark_projection.py` | E2E assigns and clears a number, toggles every mark type, deletes a selected mark, saves/reopens, and proves marks remain attached to the same durable atom with revision conflict behavior preserved. | Atom numbers and marks are not decorative transient scene items. Their parent/selection lifecycle and source-order preservation must survive reprojection. |
| FQ-018 | Edit object scale, mirrors, ordering, and properties | `actions/object_actions.py`, `dialogs/scale_dialog.py`, `models/document_session.py`, `undo/commands.py` | supported | direct document-session use; Tk none | M8-M9 WS-D, M16 WS-F | `tests/test_object_configure_undo.py`, `tests/test_object_stack_actions.py`, `tests/test_mixed_top_level_transforms.py` | Persistent mutation and undo/revision scenario through Ferrum session | M9 owns durable transaction/revision semantics, so M8a must not duplicate them. |
| FQ-019 | Clipboard copy, cut, paste, and selected SVG | `actions/edit_actions.py`, `io/clipboard_manager.py`, `io/clipboard_mime.py`, `io/export.py` | supported | session/render coupling; clipboard marker is a persistence decision, not OASA import | M8a WS-D, M12-M13 WS-C, M16 WS-F | `tests/test_clipboard_top_level_objects.py`, `tests/test_clipboard_shutdown.py`, `tests/test_structure_copy_backend_authority.py` | Round-trip supported selection and controlled shutdown without stale MIME wrappers | Retain existing ownership marker in M1b unless external consumers are migrated deliberately. |
| FQ-020 | View, theme, toolbar, keyboard shortcuts, preferences, and window state | `actions/view_actions.py`, `actions/options_actions.py`, `config/`, `themes/`, `setup/`, `widgets/`, `resource_paths.py` | supported | OASA only in `bridge/display_geometry.py`; Tk none | M1b WS-F; display geometry M11-M12 WS-C | `tests/test_keybinding_manager.py`, `tests/test_qt_theme_yaml_mapping.py`, `tests/test_theme_chooser.py`, `tests/test_zoom_controls.py` | Renamed package loads package-owned resources and preserves selected settings behavior | QSettings names remain an explicit M1b compatibility decision. |
| FQ-021 | About, keyboard help, product metadata, and release version | `actions/help_actions.py`, `dialogs/about_dialog.py`, `app.py`, `cli.py`, `versioning.py`, `bridge/release_metadata.py` | supported, but product claims are stale | direct OASA version-registry import/use in release helper; historical strings elsewhere | M1b WS-F for identity/metadata helper; M22 WS-G removes production OASA dependency | `tests/test_release_metadata_boundary.py`, `tests/test_cli_smoke_exit.py` | Correct About/CLI claims, source and installed version lookup, and product name after wheel installation | Preserve lineage acknowledgement, but do not continue claiming GPLv2, the old repository, or OASA as the shipped backend. |
| FQ-022 | Action/menu registration and plugin menu surface | `actions/action_registry.py`, `actions/registrar_manifest.py`, `actions/menu_builder.py`, `resources/menus.yaml` | supported for registered built-in actions; unsupported path for third-party plugin execution because no plugin host contract is present | OASA none in registry; Tk none | M1b WS-F, then M18/M19 WS-F if a plugin contract is adopted | `tests/test_qt_menu_contract.py`, `tests/test_qt_menu_build.py` | Every renamed registrar string resolves and all menu actions preflight; plugin path receives an explicit reproduce/fix/drop decision | Dynamic module strings are the highest rename-specific startup risk. |
| FQ-023 | OASA bridge worker lifecycle and user-visible error containment | `bridge/worker.py`, `bridge/oasa_bridge.py`, `bridge/chemistry_preparation.py`, `main_window.py` | supported during migration | direct import/use across coordinate, codec, and query work | M4d, M5, M8a, M16 WS-B/WS-D/WS-F | `tests/test_worker_lifecycle.py`, `tests/test_qt_backend_session_adapter.py` | Each replacement preserves thread confinement, result/error delivery, and revision checks | Do not replace these workers with a synchronous convenience call; the Qt thread and session boundaries are part of behavior. |

## Rename-specific closure checklist

- Move the package directory with `git mv` and rewrite imports, annotations, dynamic module
  strings, package tests, console target, package discovery, and package-data keys together.
- Start from the renamed package, open a document through the existing Qt/OASA-backed native
  CDML route, and load the resource YAML and icon directories from the installed package rather
  than the obsolete `bkchem_data` link. This is rename/start/open evidence, not Rust-backend
  adoption or worker-format completion.
- Run the root hygiene suite and the package suite separately. Root `pytest tests/` does not
  directly collect the package's nested test suite.
- Verify a built-wheel `ferrum-qt --version` and controlled offscreen startup. Package
  `conftest.py` sets `QT_QPA_PLATFORM=offscreen`.
- Add the M1e exclusion test with an empty list and a seeded violation proof only after the
  namespace settles. Add real capability paths only when their named Ferrum replacement lands.

## Limitations

This matrix records current source behavior, not a legal compatibility promise and not a
replacement implementation specification. It does not link into the gitignored OASA reference
checkout; that material remains oracle evidence for M1d and later differential reports. No
current source evidence established a known defect for a supported row, so no such classification
is invented here. The PubChem and third-party-plugin rows remain deliberately explicit decisions
rather than implied support.
