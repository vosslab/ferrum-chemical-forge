# TODO

Ferrum is at a documented delivery-stabilization checkpoint. The current
Rust-owned application, delivered contract evidence, remaining gates, and next parity package
are tracked in [ROADMAP.md](ROADMAP.md) and the canonical
[FULL_PARITY_RUST_FIRST.md](active_plans/active/FULL_PARITY_RUST_FIRST.md) ledger.

## Resume first

- Record maintainer native keyboard/accessibility sign-off separately from the
  delivered action bridge. Do not restore per-atom Qt durable-item identity or
  a Python structural-selection model.
- Obtain maintainer visual sign-off on the regenerated 13-image candidate group
  when preparing a release. Source review, semantic capture checks,
  image-by-image agent visual review, the fresh aggregate validation, and the
  seven independent post-fix reviews are complete.
- Reconcile broader historical milestone and receipt status in the canonical
  parity ledger. `ActionRegistry` lifecycle retirement and the nominal
  `DocumentDisplayRefreshableV1` contract are delivered and independently
  accepted at code level; full parity remains open.

## Bounded slice status

- M2.B, M4.C, the Rust-projected periodic picker, and the M6 tab-owned opaque
  Rust selection-fence bridge have focused evidence but do not close M2, M4,
  or M6. Their ownership and evidence are recorded in the canonical parity ledger.

## Next parity implementation

- Complete approved in-progress `PARITY-M5.A`
  [Template Catalog V1](active_plans/decisions/m5_template_catalog_v1.md). It is an independent
  Rust snapshot/placement boundary that consumes delivered document
  admission and fences without expanding the open M2/M4 corpora.
- Keep the Rust catalog authoritative: opaque keys/content identity, canonical shipped recipe
  descriptors, provenance, compatibility, explicit entry/candidate/refusal/file/total-byte limits,
  bounded lexical admission, aggregate refusal occurrences, and frozen PyO3 projection.
- Keep placement fenced by the native-issued expected document snapshot, and keep publication as
  a Rust prepared capability/receipt. Qt's modeless dialog/tab/window task exposes only
  `chemistry.template.catalog`; it has no scanner, re-admission, payload, plan, or raw OS errors.
- Automated M5.A evidence is green: build CLI/GUI; 13 catalog, 164 API, 8 PyO3, and 18 Qt focused
  tests; public authoring E2E `ok`; workspace test/strict Clippy; and aggregate 8,092 hygiene,
  all registered E2Es, 294 installed PyO3, and 344 Qt tests. Three final reviews found no P1/P2/P3.
- Keep M5.A open for manual native accessibility/contrast/focus review and a fresh real-dialog
  screenshot with human acceptance; full parity remains open.
- Execute Patch 1 of the approved
  [Qt Operation Lease Registry](active_plans/decisions/qt_operation_lease_registry.md): Template
  Catalog explicit controller plus pure Qt lifecycle registry. Delete both catalog mixins and
  `CATALOG_PLACEMENT_BLOCKED`; tab close cancels synchronously and continues the same attempt.
  Make no Rust/PyO3 change. Require independent review, focused registry/controller/lifecycle
  evidence, full Qt, public template E2E, and aggregate acceptance before Patch 2.
- Only after Patch 1 accepts, execute Patch 2: Local Document Open controller with source-retaining
  `CANCELLATION_REQUESTED` and truthful delivery cancellation. Do not add aliases, duplicate state,
  an event bus, a service locator, or a wholesale tree move.
